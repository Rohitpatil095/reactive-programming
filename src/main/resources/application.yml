spring:
  data:
    mongodb:
#      authentication-database: admin
#      username: root
#      password: root
      database: webflux
      port: 27017
      host: localhost
      
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG